import { AmqpConnectionManagerOptions } from 'amqp-connection-manager';

/**
 * Tipo que define las opciones adicionales de configuración
 * para la conexión de RabbitMQ (wrapper sobre `amqp-connection-manager`).
 */
export type RabbitMQOptions = AmqpConnectionManagerOptions;
